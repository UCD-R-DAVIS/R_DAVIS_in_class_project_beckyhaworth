#AI assisted coding
#Claude in Cursor, GitHub Co Pilot in Rstudio
#Ctrl K - to open chatbox
#example - Query the U.S. EPA SDWIS API (safe drinking water) for a list of all Community Water Systems in Alaska
#you have to know what you're asking for, specify as much as you can

#highlight everything, then hit Control K again...then ask "Now turn this into a function where i can specify a state abbreviation" 

#highlight everything, control K, "Change this so that it downloads the dataset of systems and stores it in a directory that i specify"

#highlight everything, control K, "Create a CLOBBER option where if CLOBBER = T, the file is overwritten, but if the file already exists and CLOBBER = F, it skips"